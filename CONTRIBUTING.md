# 贡献指南

感谢你愿意为 **数据要素 DX Skillkit** 添砖加瓦。

本套件不是"提示词合集"，而是一套**可执行、可验收的分析流水线**。因此对贡献的要求也围绕这一点展开：**能跑、能验、能复现**，比写得漂亮重要得多。

---

## 一、贡献什么最有价值

按价值从高到低排序：

| 优先级 | 贡献类型 | 说明 |
|---|---|---|
| ⭐⭐⭐ | **真实案例（含 gotchas）** | 脱敏后的实战案例，尤其是"踩过的坑"——这是套件最稀缺的资产 |
| ⭐⭐⭐ | **门禁规则补强** | 在 `_shared/scripts/gate_check.py` 里补一条能挡住真实错误的新检查 |
| ⭐⭐ | **口径一致性条目** | 在 `_shared/assets/口径一致性校验表.md` 里补一条"同一个数字在几处必须一致" |
| ⭐⭐ | **政策索引更新** | 数据要素政策更新快，`_shared/references/数据要素政策索引.md` 需要人盯 |
| ⭐ | **技能正文优化** | 正文更短更准、步骤更可执行、反例更具体 |
| ⭐ | **文档/错别字** | 欢迎，但请同 PR 只做这一件事 |

**不接受的贡献**：泛泛而谈的方法论扩写、把正文改长但不增加可执行性、纯靠感觉的"最佳实践"。

---

## 二、案例贡献规范（重点）

案例是套件的核心资产，也是最容易贡献失当的部分。请严格遵守：

### 2.1 目录结构

在 `cases/` 下新建目录，命名格式 `案例代号-行业`（如 `sample-redacted-manufacturing`）：

```
cases/<slug>/
├── README.md            # 案例概述：背景、边界、结论
├── engagement.md        # 立项信息：委托方类型、目标、范围、时间
├── baseline-summary.md  # 现状摸底结论（对接 dx-data-baseline）
└── summaries.md         # 各环节交付物摘要（对接后续技能）
```

可直接复制 `cases/_template/` 起步。

### 2.2 脱敏红线（必须遵守）

提交前逐条自检，**任何一条不满足都请勿提交**：

- [ ] 无真实企业名称（用"某省级农批集团"这类泛化表述）
- [ ] 无真实人名、工号、联系方式
- [ ] 无未公开的财务数字（金额可做同比例缩放，须在 README 注明"数字经等比缩放"）
- [ ] 无系统截图、报表原图、组织结构原表
- [ ] 无商标、Logo、可识别的水印
- [ ] 无具体客户合同条款、报价
- [ ] 若数字来自公开财报，须在 README 注明来源与年份

参考 `cases/CASE-PRIVACY.md` 的完整脱敏清单。

### 2.3 必须附带 gotchas

每个案例的 `README.md` 必须含一节 `## 踩过的坑`，至少 2 条真实教训，格式：

```markdown
## 踩过的坑

- **现象**：均价对不上，差 3% 左右。
  **根因**：两个部门各有一套口径，一个含税一个不含税。
  **修正**：在口径一致性校验表里新增一条，所有对外数字强制标注口径。
  **代价**：汇报前夜返工，多花 1.5 人日。
```

没有 gotchas 的案例会被要求补充。—— 顺利的案例没有教学价值，**踩坑才是**。

---

## 三、代码/脚本贡献规范

### 3.1 脚本

`_shared/scripts/` 下的脚本是套件的执行底座，改动请遵守：

- **零重依赖**：仅用标准库 + 已声明依赖，新增依赖须在 PR 里说明理由并更新 README
- **可离线跑**：不得依赖外部 API 才能出结果
- **失败要出声**：检查不通过必须 `sys.exit(1)` 并打印具体条目，禁止静默跳过
- **自测**：PR 须附一次实际运行输出（贴关键行即可）

### 3.2 技能正文（SKILL.md）

改 `SKILL.md` 时请注意：

- **frontmatter 五件套齐全**：`name` / `description` / `description_en` / `license` / `allowed-tools` / `metadata`
- **`description` 控制在 75 字符内**（中文按字符计），超长会被渐进式披露截断
- **正文增量 < 5000 token**（中文约 5000 字），超了就拆到 `references/`
- **每条规则要有"后果"**：说明不这么做会发生什么，不要只写"应该怎样"
- **数字口径**：正文里出现的任何数字，必须能在 `_shared/assets/口径一致性校验表.md` 找到出处

### 3.3 视觉资产

README 引用的图必须是**可复现生成**的，不能是手工截图。当前生成脚本：

```bash
cd docs
python _make_contact_sheet.py   # → docs/images/deck-overview.png
python _make_architecture.py    # → docs/images/architecture.png
```

改了设计令牌或页数，请重跑两个脚本并提交新 PNG，同时提交脚本变更。

---

## 四、提交前自检清单

复制到 PR 描述里逐项打钩：

- [ ] `python _shared/scripts/gate_check.py` 本地跑一遍，通过
- [ ] 新增/修改的数字已进 `口径一致性校验表.md`
- [ ] 案例已过脱敏红线全部 7 条
- [ ] 案例含 `## 踩过的坑` 且 ≥2 条
- [ ] `CHANGELOG.md` 已在 `[Unreleased]` 段落记录本次变更
- [ ] 如改首页内容，README 与 `docs/images/` 已同步重建
- [ ] 提交信息格式：`<type>(<scope>): <描述>`，例如 `feat(dx-data-quality): 新增空值率硬门禁`
      type ∈ `feat` / `fix` / `docs` / `refactor` / `chore`

---

## 五、分支与提交

- 分支命名：`feat/xxx`、`fix/xxx`、`docs/xxx`
- 一次提交只做一件事，避免"顺便"改无关文件
- 涉及方法论口径变更（如七道工序定义）的，请在 PR 描述里说明影响面，这类改动需 maintainer 明确同意

---

## 六、行为准则

- 对事不对人，技术分歧用证据说话
- 不提交无法复现的结论
- 尊重案例提供方的商业秘密——一次泄露，套件信誉归零

---

有问题开 issue，描述里请带上：你的使用场景、期望结果、实际结果、复现步骤。诊断类 issue 请贴完整报错。
