# 怎么用 · 怎么试效果（HOW-TO-USE）

> 面向使用者（非程序员也看得懂）。三步：**装上 → 说一句话 → 看产出。**

---

## 一、安装（二选一，拷贝即可）

套件是**纯 Markdown + 两个零依赖 Python 脚本**，没有数据库、不需要联网、不装任何包。

### 方式 A：用户级（推荐，所有项目都能用）
把 7 个 `dx-*` 技能目录 + `_shared` 拷到用户技能目录：

```bash
# 在 K 盘仓库根目录执行
cp -r data-element-dx-skillkit/dx-engagement-orchestrator \
      data-element-dx-skillkit/dx-industry-research \
      data-element-dx-skillkit/dx-competitor-scout \
      data-element-dx-skillkit/dx-data-baseline \
      data-element-dx-skillkit/dx-architecture-opt \
      data-element-dx-skillkit/dx-data-quality \
      data-element-dx-skillkit/dx-application-plan \
      ~/.workbuddy/skills/
```
Windows 下可用资源管理器直接拖拽到 `C:\Users\Administrator\.workbuddy\skills\`。

> ⚠️ `_shared/` **不要**单独拷进 skills 目录（它不是技能，没有 SKILL.md，会被当成坏技能）。
> 技能里对 `_shared` 的引用是**相对路径** `../_shared/...`，所以**技能目录与 `_shared` 必须保持同级**。

### 方式 B：项目级（只在某个项目里生效）
拷到 `<项目目录>/.workbuddy/skills/`，同样保持 `_shared` 同级。

### 方式 C：不安装，直接在本仓库用（**试效果最省事，推荐先这样**）
不做任何拷贝，直接在这个工作区（`K:\workbuddy\星级SKILL- gihub`）里对话。
因为技能就在当前工作区下，我读取 `SKILL.md` 的路径是通的。试完满意再走方式 A。

---

## 二、触发方式：说人话就行

技能靠 `description` 里的**触发词**命中，不需要记技能名。下面这些句子都能触发：

| 你想干的事 | 直接说 |
|---|---|
| 盘家底 | "帮我盘一下集团有什么数据""做个数据资产盘点""我们数据现状摸底" |
| 摸成熟度 | "做个 DCMM 成熟度初评" |
| 定架构 | "数据底座怎么建""出个数据架构优化方案" |
| 治数据 | "数据质量怎么提升""建立指标字典""统一口径" |
| 找场景 | "数据能干什么""规划数据产品""数据资产怎么入表" |
| 汇报 | "给董事长做个汇报 PPT""数字底座总体方案" |
| **全套** | "做一次数字化转型整体方案交付""七道工序全景" |

想**强制走总控**（推荐第一次试），直接把 orchestrator 点名：

> "用 **dx-engagement-orchestrator** 给我走一遍：以【某地农批集团】为主体的数据要素数字化转型交付。"

---

## 三、试效果：三个最小示例（从易到难）

### 示例 1｜只试一个技能（3 分钟，最轻）
> "用 dx-data-baseline 帮我做数据现状摸底。主体代号 `集团A`。
> 我手上有的线索：一个 Excel 目录 `D:\测试数据`，里面有 3 个 CSV。
> 先跑一遍脚本化盘点，再给我一份系统清单 + 资产清单 + 成熟度初评的骨架。"

**预期产出**：
- `assets.csv`（脚本扫出来的真实文件清单）
- `系统清单_集团A_2026-09-12.md`
- `数据资产清单_集团A_2026-09-12.xlsx`（17 列骨架，待你补业务含义）
- `DCMM成熟度初评_集团A.md`

> 注意：摸底是"事实清点"，**产出的是骨架 + 待确认清单**，不是我想象的完美答案。你填进去的业务信息才是价值所在。

### 示例 2｜试编排 + 门禁（15 分钟，看"流程感"）
> "用 dx-engagement-orchestrator 立项，主体是某线下平台型集团（国有）。
> 驱动因素：集团要做数据资产入表。
> 我先只做外部洞察 + 摸底两步，摸底先给骨架，别往下走。"

**预期产出**：
- `交付计划_集团A_2026-09-12.md`（阶段 × 技能 × 交付物 × 门禁条件）
- `门禁报告_第2阶段_2026-09-12.md`（会**明确告诉你缺什么**，这一份最能体现套件价值）
- 停在摸底阶段，**不擅自往下推**——这是编排层的核心纪律。

### 示例 3｜试应用与呈现（试汇报文风）
> "dx-application-plan：假设前面摸底/架构/质量都完成了，帮我给董事长出一版汇报框架。
> 要求：先价值后技术；我用虚构数据填数字，你只给结构和话术。"

**预期产出**：约 20 页的汇报结构（现状困境 → 价值量化 → 实施路径 → 快速见效 → 投入保障 → 风险 → **决策事项**）。

---

## 四、一次完整交付长什么样

```
第 0 步  立项            → 交付计划 + 案例探测
第 1 步  外部洞察（可并行）→ 行业研究报 + 对标矩阵
第 2 步  摸底 ★关键门禁   → 系统清单 + 资产清单 + 数据流图 + DCMM 分
第 3 步  架构            → 底座蓝图 + 路线图 + 治理卡口检查表
第 4 步  质量            → 指标字典 + 数据标准 + 质量规则集
第 5 步  应用与呈现       → 场景地图 + 产品清单 + 汇报PPT
第 6 步  收口            → 口径一致性校验 + 七道工序全景图 + 案例沉淀
```

**每一阶段结束都会有一份"门禁报告"**，四项检查：
① 交付物存在吗 → ② 是真空还是空壳 → ③ 挂回七道工序了吗 → ④ 数字跟上一阶段对得上吗。

> 门禁这一层，是这套东西跟普通"帮我写个方案"最大的区别。

---

## 五、两个脚本怎么单独跑

```bash
# 1) 资产扫描（零依赖，枚举 CSV/Excel/Parquet/DB，输出表头与选型建议）
python _shared/scripts/scan_assets.py <目录> --out assets.csv

# 2) DCMM 8 域成熟度评分（零依赖，1-5 分制）
#    先写 scores.json：{"数据战略":2,"数据治理":2,"数据架构":2,"数据应用":2,
#                       "数据安全":1,"数据质量":2,"数据标准":1,"数据生命周期":2}
python _shared/scripts/maturity_score.py scores.json
```
两个脚本都**实跑验证过**，输出加权总分 + 等级判定 + 雷达数据。

---

## 六、看全景图

直接双击打开（或浏览器拖入）：

```
K:\workbuddy\星级SKILL- gihub\data-element-dx-skillkit\全景图.html
```

一页看完全套：七道工序 / 技能映射 / 交付链 / 门禁四项 / 场景四象限 / 四条差异化 / 融合来源。
**纯本地单文件 HTML，无外链无依赖**，可以直接发给别人看。

---

## 七、案例库怎么用（这决定套件会不会越用越值钱）

- `cases/_template/` — 空白模板，每接一个新项目就复制一份改名。
- `cases/sample-redacted/` — 虚构样例（集团A），看格式用的。
- `cases/<你的真实项目>/` — 已被 `.gitignore` 拦截，**不会进公开仓**。

一套案例包 = 五份摘要：立项信息 / 摸底摘要 / 各技能 summaries / gotchas。
写案例的价值在于：**下一个项目直接复用上一个项目踩过的坑**。

详细保密机制见 `cases/CASE-PRIVACY.md`。

---

## 八、当前成熟度（v2.0：7 个技能全部生产级）

| 技能 | 状态 | 能直接用的程度 |
|---|---|---|
| dx-engagement-orchestrator | ✅ v2.0 生产级 | 可跑流程与门禁 |
| dx-data-baseline | ✅ v2.0 生产级 | 可跑脚本 + 出骨架 |
| dx-architecture-opt | ✅ v2.0 生产级 | 可出七层蓝图 + 路线图 + HALT 卡口表 |
| dx-data-quality | ✅ v2.0 生产级 | 可建指标字典/标准/主数据/规则集/访问策略六件套 |
| dx-application-plan | ✅ v2.0 生产级 | 可出场景地图/产品/入表路径/汇报结构 |
| dx-industry-research | ✅ v2.0 生产级 | 可出行业研究报 + 政策机会清单 + 图表要点 |
| dx-competitor-scout | ✅ v2.0 生产级 | 可出四维对标矩阵 + 可抄作业清单 |

> **如实说明**：骨架与方法论已全部完整，且每个技能都带了 8–13 条实战 gotchas。
> 但"**项目专属的实战细节**"需要真实案例喂养——这部分**只能由你提供脱敏素材**，我不能凭空编。
> 也就是说：**现在可以拿去跑真项目了**，跑完把踩的坑回写进案例层，套件会越用越准。
